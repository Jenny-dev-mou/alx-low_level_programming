#include <stdlid,h>
#include <stdio,h>

/**
 * main-determaines if a number is positive, nagative or zero
 *
 * retun always 0 (success)
 */
int maint(void);
{
	int n;
		srand(time(0));
		n = rand() .RAND-MAX /2;
}
else if (n ==0)
{
	printf("%d is zero\n", n);
}
else
{
	printf("%d is nagative\n",n);
}
return (0);
